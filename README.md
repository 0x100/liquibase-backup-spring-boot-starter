# spring-boot-liquibase-backuper-starter
Spring Boot starter for backing up data in runtime with the Liquibase (generating changesets with "inserts") from a DB specified as datasource in the application
